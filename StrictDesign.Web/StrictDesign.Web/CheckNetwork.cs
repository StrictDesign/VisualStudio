using System.Net.NetworkInformation;

namespace StrictDesign.Web
{ 
    public class CheckNetwork : NetworkInterface
    {
        //long beginValue = GetIPv4Statistics().BytesReceived;
        //DateTime beginTime = DateTime.Now;

        //// do something

        //long endValue = GetIPv4Statistics().BytesReceived;
        //DateTime endTime = DateTime.Now;

        //long recievedBytes = EndValue - BeginValue;
        //double totalSeconds = (endTime - beginTime).TotalSeconds;

        //long bytesPerSecond = RecievedBytes / TotalSeconds;
        //private static int TotalSeconds;

        //public static int RecievedBytes { get; private set; }
        //public static int EndValue { get; private set; }
        //public static int BeginValue { get; private set; }
    }
}